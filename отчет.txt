Отчет.
Студент Шкляр Станислав Станиславович
Группа БПИ-214
Вариант 12
Условие задачи:
    Разработать программу, определяющую минимальный и максимальный (по числовому значению) символ в заданной ASCII-строке.  
Задание выполнено с расчетом на оценку 6

На 4 балла:
    * В файле 4.c хранится программа на языке C.
    * Получена ассемблерная программа(4.s) откомпилированная при помощи команды в командной строке: gcc -O0 -Wall -masm=intel -S -fno-asynchronous-unwind-tables -fcf-protection=none main.с  
    * Итоговая ассемблерная программа откоплилирована и скомпонована командой
        gcc 4.s -o s
      Программа на языке C откоплилирована и скомпонована командой
        gcc 4.c -o c
          
        
На 5 баллов:
    * Реализована программа(5.c) с использованием функции с передачей данных через параметры
    * В функции Max используются локальные переменные:
        int i
    * В функции Min используются локальные переменные:
        int i
      В функции main используются локальные переменные:
        char max = 0, min = 127;
    * Получена ассемблерная программа(5.s), откомпилированная при помощи команды в командной строке
        gcc -O0 -Wall -masm=intel -S -fno-asynchronous-unwind-tables -fcf-protection=none 5.c -o 5.s
    * В полученную ассемблерную программу добавлены комментарии, описывающие передачу фактических парметров, перенос возвращаемого результата. Описана связь между параметрами языка C и регистрами
    

На 6 баллов:
    * Минимизировано количество обращений к памяти путем максимального использования регистров процессора, были произведены замены:
         DWORD PTR [rbp-20] - eco
         BYTE PTR [rbp-1] - bh
         BYTE PTR [rbp-2] - bl
* Тесты:
        Hello, world!
        
        
         w
        
        Hello,12345!@#$
	!
	o

         aaaaaaaaaaa
       
	a
	a

    * Результат тестовых прогонов программы на C и Ассемблере хранится в фотографиях в папкax 5, 6.

        


